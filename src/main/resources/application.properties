spring.application.name=v2x-tools-engine
server.port=${PORT:8090}
# El backend llega al engine por su URL pública (WIND_ENGINE_URL), así que el binding por
# defecto (0.0.0.0) alcanza. Si algún día se vuelve a la red privada de Railway (IPv6-only,
# *.railway.internal), agregar:  server.address=::  (ver gotchas en v2x-tools/CLAUDE.md).
